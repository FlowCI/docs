# Agent Scaling On Host via SSH

Agent scaling enable to optimize ci flow performance

## Introduction

Setup host with ssh credential, ci server could scaling agent on hosts

![](./img/ssh_agent.png)